<h1>Your list that you want to do on the vacation</h1>
<ul>
<% @lists.each do |list| %>
<li><%= link_to list.title, list_path(list)%>
<%= link_to list_path(list), data: {turbo_method: :delete, turbo_confirm: "Are you sure?"} do %>
<i class="fa fa-trash-can"></i>
<% end %>
</li>
<% end %>
</ul>
<%= link_to "+add new list", new_list_path %>


<h1>Your list that you want to do on the vacation</h1>
<ul>
<% @lists.each do |list| %>
<%# <li><%= link_to list.title, list_path(list)%>
<li><%= link_to list.title, list_path(list)%>
<%=  list.details %>

<%= link_to "Delete",
  list_path(list),
  data: {turbo_method: :delete, turbo_confirm: "Are you sure?"}
%></li>
<% end %>
<%= link_to "+add new list", new_list_path %>
</ul>
